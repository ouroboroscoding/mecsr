# auth service
upstream auth_service {
	server localhost:8002 fail_timeout=0;
}

# webpoll service
upstream webpoll_service {
	server localhost:8006 fail_timeout=0;
}

server {

	if ($scheme != 'https') {
		return 301 https://$host$request_uri;
	}

	listen 80;
	include ssl_params;
	server_name services.mecsr.com;
	access_log /var/log/mecsr/services.access.log;
	error_log /var/log/mecsr/services.error.log;

	# auth
	location ~ ^/auth(/?)(?<noun>.*) {
		proxy_pass http://auth_service/$noun$is_args$args;
		proxy_redirect off;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_read_timeout 600;
	}

	# webpoll
	location ~ ^/webpoll(/?)(?<noun>.*) {
		proxy_pass http://webpoll_service/$noun$is_args$args;
		proxy_redirect off;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_read_timeout 600;
	}
}
